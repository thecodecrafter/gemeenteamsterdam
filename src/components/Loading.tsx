import { Icon } from "@amsterdam/design-system-react";
import { SpinnerIcon } from "@amsterdam/design-system-react-icons";

export const Loading = () => {
  return <Icon className="spinner" svg={SpinnerIcon} />;
};
